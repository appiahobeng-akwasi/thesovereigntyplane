/**
 * Placeholder for type generation from Supabase.
 * Run `pnpm generate-types` to regenerate types.ts from the Supabase schema.
 *
 * In production, use:
 *   npx supabase gen types typescript --project-id nxfuthsmgxzjvehroduo > src/data/types.ts
 *
 * For now, types are maintained manually in src/data/types.ts.
 */
console.log(
  'Type generation placeholder. Run supabase gen types to regenerate from schema.'
);
